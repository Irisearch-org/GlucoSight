# inti pipeline (predict, schema, macro lookup)
Pillow>=10.0.0
# PyTorch 2.2.x yang dipakai baseline memerlukan NumPy 1.x untuk bridge tensor.
numpy>=1.26.4,<2

# hanya dibutuhkan untuk inference model sungguhan
torch>=2.0.0
torchvision>=0.15.0
