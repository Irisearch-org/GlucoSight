Folder output feature file. Diisi otomatis oleh scripts/run_batch.py.

  features.jsonl    satu record JSON per baris, join by sample_id
  run_summary.json  hitungan valid/invalid + sebaran feature_status

Folder ini sengaja dikosongkan di paket ini — isinya harus dihasilkan dari
gambar meal asli, bukan dari mode --mock.
