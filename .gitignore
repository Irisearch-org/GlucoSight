# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# ── Virtual environments ────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Jupyter ─────────────────────────────────────────────
.ipynb_checkpoints/

# ── Raw data — NEVER commit (see CLAUDE.md, DVC is in the stack) ──
# Third-party datasets and participant data are distributed
# out-of-band (DVC / shared drive), not through git.
#
#   rppg/PPG_Dataset/RawData  — PPG glucose set, 23 subjects, licence unconfirmed
#   rppg/PPG_Dataset/Labels   — glucometer ground truth
#   rppg/PPG_Dataset/Figures  — waveform plots
#   rppg/PPG_Dataset/brno     — BUT PPG (CC-BY 4.0)
#
# This repo is PUBLIC. Human-subjects physiological data does not go in it.
rppg/PPG_Dataset/RawData/
rppg/PPG_Dataset/Labels/
rppg/PPG_Dataset/Figures/
rppg/PPG_Dataset/brno/

# Participant data collected in Surabaya — never commit
# Anchored to repo root: this is the top-level data/{track}/ raw-data
# directory from CLAUDE.md's repo layout, NOT track-internal dirs like
# rppg/data/ (that one holds pipeline code + derived labels.csv, which
# IS committed — see rppg/data/convert_labels.py).
/data/
**/participant_data/
*.mat
*.dat
*.hea

# Model weights and checkpoints
*.pt
*.pth
*.ckpt
*.h5
*.onnx
checkpoints/
runs/
wandb/

# Results that can be regenerated
results/
*.png
*.jpg
*.jpeg
!docs/**/*.png

# ── DVC ─────────────────────────────────────────────────
/.dvc/cache
/.dvc/tmp

# ── OS / editor ─────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
