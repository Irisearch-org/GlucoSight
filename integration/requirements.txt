# Integration layer + MVP endpoint
numpy>=1.24
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Testing
pytest>=7.4
httpx>=0.27

# Pulled in by the tracks the adapters call:
#   scipy       — rppg/features/extractor.py
#   pandas      — nlp/, cv/ batch scripts
#   torch, timm — cv/cv_baseline/classifier.py (only when a photo is supplied)
